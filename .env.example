# Kopiere diese Datei nach `.env` und trage echte Werte ein.
# `.env` nicht committen (sollte in .gitignore stehen).

# -----------------------------------------------------------------------------
# Google AI Studio / Gemini API
# -----------------------------------------------------------------------------
# Benötigt für: Bildgenerierung mit dem Skill `gemini-image-generator`
#   → `uv run python .cursor/skills/gemini-image-generator/scripts/generate_image.py`
# Schlüssel anlegen: https://aistudio.google.com/apikey
GEMINI_API_KEY=
